version: "2"
sql:
  - engine: "postgresql"
    schema: "sql/schema.sql"
    queries:
      - "sql/users/*.sql"
    gen:
      go:
        package: "db"
        out: "internal/db"
        sql_package: "pgx/v5"
